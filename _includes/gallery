
<section id="{{name}}" class="gallery {{name}} {{ include.data.class }}">
	<div class="container-fluid">

		{%- assign subject = site.subjects | where: "relative_path", include.data | first -%}
		
		<div class="section-title">
			{%- if subject -%}
				{{ subject.content }}
			<!-- { %- else -%} -->
				<!-- { { include.data | markdownify }} -->
			{%- endif -%}
		</div>
		
		
		<div class="row no-gutters">
			
			{%- if include.data.mode == 'masonry' -%}
				<div class="masonry-grid">
			{%- endif -%}
			
			{%- if subject -%}
				{%- assign photos = subject.photos -%}
			{%- else -%}
				{%- assign photos = include.data.photos -%}
			{%- endif -%}
			
			{% for photo in photos %}
			<div class="col-lg-3 col-md-6 mt-4 gallery-item">
				<a href="{{ photo }}" class="gallery-lightbox">
					<img src="{{ photo }}" alt="" class="img-fluid">
				</a>
			</div>
			{% endfor %}

			
			{%- if include.data.mode == 'masonry' -%}
				</div>
			{%- endif -%}

		</div>

	</div>
</section>

